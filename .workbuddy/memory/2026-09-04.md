# 2026-09-04 工作记录

## 任务执行引擎 P0–P2 完成并提交
- 在分支 `workbuddy`（注：git 分支名不能以 `/` 开头，用户说的 `/workbuddy` 落地为 `workbuddy`）完成 P0–P2 并推送到 origin。
- commit `2f4db86`，15 文件，+1045/−60。
- 后端：新增 `WorkItemRun` append-only 表 + `execution_graph.py`（collapse_runs/ready/startable/graph_depths/available_actions）+ `query_service.py` 状态投影 + `execution_service.py`（prepare 预检/materialize 复检、依赖闭环校验）+ workflow/types/command_service/sessions 注册 `start_task`/`complete_task`/`fail_task`。
- 前端：dto 加 `available_actions?`/`graph_depth?`；卡片按后端下发动作渲染按钮并只转发；Flow 视图按 DOM 实测坐标画依赖 DAG 连线（SVG 覆盖层）；泳道补 failed 状态。
- 测试：后端 448 passed / 5 failed（5 个失败是**既有基线** bug：3×ROOT suggested_assignee 期望 approver-1 实际 owner-1，2×outsider 本应 403 实际 200 放行）。新增 e2e `test_work_item_execution.py` 10 用例全过，含「完成依赖解锁后继」端到端证明。前端 `tsc --noEmit` 0 错误。
- P3（Agent 真跑任务，需先把 process-local 的 `_ACTIVE_PREPARATIONS` 租约外置为分布式锁）按用户要求暂缓，未实现。

## 提交注意事项
- 排除项：`backend/.tmp/plugins` 是个无 `.gitmodules` 注册的 gitlink（"contains modified content"），非本次改动，未纳入提交。
- 既有基线测试失败（A/B 两类）与本次改动无关，已在 `docs/任务执行引擎实施计划.md` 与图引擎调研中记录；若日后要动权限/assignee 需先修。
