# firstFlight 工程任务规划规则

收到软件任务、完成必要澄清后，在生成阶段、里程碑或任务计划前，先读
`docs/sdlc/README.md`，加载 `backend/rules/sdlc/selection.yaml`、
`backend/rules/sdlc/execution.yaml` 和对应 `models/*.yaml`。
这套规则是本仓库生命周期规划的总纲；原始来源为 `docs/sdlc/source.md`。

- 依据已确认需求回答 10 项模型选择问题；未知不等于否。不确定性影响路线、
  多路线冲突或没有匹配时，说明缺失决策，完成澄清后再拆分，禁止默认套模板。
- 保留所选模型的阶段编号、四字以内名称、阶段数量和迭代结构。每个阶段
  必须有准入、核心工作、交付物、准出评审、责任人和排期，任务映射必须完整。
- 将门禁建成任务依赖，执行 YAML 中的并行边界。里程碑是容器，不能作为
  可执行任务的依赖目标。规划时可生成未来阶段的任务说明，不能声称正式工作已启动。
- 用户已确认的范围和授权有效。SOP 不授予生产发布、签字或范围扩张权限。
  既有 PRD/评审证据可以复用为输入；缺少证据时不得伪造完成状态。
- 缺陷与变更按所选模型的 `defects` / `changes` 规则规划影响分析、暂停、
  回退、重评和恢复条件。明确源头阶段、关联模块、责任人及记录位置。
- 修改规则时更新解释与样例，运行 SDLC 单元和集成测试。不得通过关闭校验、
  删阶段、删交付物、补造选择证据等方式让违规计划通过。
- 日常小问题的回答、原样转述和只读检查不需要额外生命周期计划。
