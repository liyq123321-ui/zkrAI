# 多项目共享看板实施计划

目标：新建任务仅切换左侧对话；右侧看板、流转图和审计汇总数据库全部项目，刷新后重新加载。

约束：按用户补充要求增加只读 GET /sessions 数据库目录接口；不新增子 Agent 执行或员工指派流程。

设计：新增 `useWorkspaceProjects.ts`，按 Session ID 管理独立的状态和资源快照，当前对话 ID 与项目集合分离。加载项目只更新对应快照，不改变当前对话。详情与 PRD 命令使用所选任务所属项目的状态，避免跨项目串用 PRD、状态版本和人工指令草稿。

- [x] 在 `workspace.test.tsx` 复现打开新任务后看板消失的问题，并覆盖创建第二个项目、刷新恢复多个项目、旧项目详情及项目加载失败。
- [x] 新增数据库项目目录 API 和项目集合管理，处理失效项目和独立加载失败；浏览器不保存项目名单。
- [x] 修改 `ApiWorkspace.tsx`：左侧项目切换与新建表单，右侧汇总资源、Root 名称下拉多选、项目标签、刷新看板；详情与命令绑定所属项目。
- [x] 更新用户指南、需求和架构说明，运行前端测试、类型检查、构建以及实际浏览器验证。

结果：前端 54 项测试、类型检查、生产构建通过；后端 23 项 Session 接口测试通过，包含空目录、数据库所有 Root 和只读副作用断言。浏览器核对 2 个 Root、3 个里程碑、12 个子任务及多选/新建时保留。独立复核未发现阻断问题。

验收命令：`npm test`、`npm run lint`、`npm run build`，目录为 `frontend/aios-main/`。完成后提醒保存 Git，不自动提交或推送。
